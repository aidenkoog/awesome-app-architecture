export default class UserData {
    constructor(photoUrl, name, gender, birthday, height, weight) {
        this.photoUrl = photoUrl
        this.name = name
        this.gender = gender
        this.birthday = birthday
        this.height = height
        this.weight = weight
    }
}