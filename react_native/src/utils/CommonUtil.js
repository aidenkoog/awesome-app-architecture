/**
 * check if the data is valid.
 * @param {string} data 
 * @returns 
 */
export const isValid = (data) => {
    if (item == null || item == "" || item == "undefined") {
        return false
    }
    return true
}