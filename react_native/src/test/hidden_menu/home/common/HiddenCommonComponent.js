import { View, Text } from "react-native"

/**
 * component ui that is used in container.
 * @returns {JSX.Element}
 */
export default function HiddenCommonComponent() {
    return (
        <View>
            <Text> Hidden Common </Text>
        </View>
    )
}