import { StyleSheet } from 'react-native'

/**
 * declared stylesheet globally and components can use it.
 */
export default StyleSheet.create(
  {
    // home bottom tab navigation screen styles
    homeBottomTabIconMenu: {
      width: 32,
      height: 32,
    },
    homeBottomTabBar: {
      height: 70,
      paddingTop: 12,
      paddingBottom: 10.6
    }
  }
)