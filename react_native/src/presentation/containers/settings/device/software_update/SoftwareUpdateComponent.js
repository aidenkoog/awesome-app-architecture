import { View, Text } from "react-native"

/**
 * component ui that is used in container.
 * @returns {JSX.Element}
 */
export default function SoftwareUpdateComponent() {
    return (
        <View>
            <Text> SoftwareUpdate </Text>
        </View>
    )
}