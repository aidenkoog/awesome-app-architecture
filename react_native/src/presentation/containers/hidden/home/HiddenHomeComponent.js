import { View, Text } from "react-native"

/**
 * component ui that is used in container.
 * @returns {JSX.Element}
 */
export default function HiddenHomeComponent() {
    return (
        <View>
            <Text> Hidden Home </Text>
        </View>
    )
}