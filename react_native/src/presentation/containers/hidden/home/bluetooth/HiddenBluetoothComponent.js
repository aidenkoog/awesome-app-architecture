import { View, Text } from "react-native"

/**
 * component ui that is used in container.
 * @returns {JSX.Element}
 */
export default function HiddenBluetoothComponent() {
    return (
        <View>
            <Text> Hidden Bluetooth </Text>
        </View>
    )
}