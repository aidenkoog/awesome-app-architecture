import { View, Text } from "react-native"

/**
 * component ui that is used in container.
 * @returns {JSX.Element}
 */
export default function HiddenSplashComponent() {
    return (
        <View>
            <Text> Hidden Splash </Text>
        </View>
    )
}