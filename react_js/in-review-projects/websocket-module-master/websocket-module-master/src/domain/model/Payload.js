class Payload {
  constructor(timeStamp, headerMessage, bodyMessage) {
    this.timeStamp = timeStamp
    this.headerMessage = headerMessage
    this.bodyMessage = bodyMessage
  }
}

export default Payload